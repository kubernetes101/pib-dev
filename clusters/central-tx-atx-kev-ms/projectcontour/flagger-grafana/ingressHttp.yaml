apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flagger-grafana
  namespace: projectcontour
  annotations:
    ert-manager.io/cluster-issuer: letsencrypt-prod
    ingress.kubernetes.io/force-ssl-redirect: "false"
    kubernetes.io/ingress.class: contour

spec:
  rules:
  - host: 20.169.44.36
    http:
      paths:
      - pathType: Prefix
        path: /grafana
        backend:
          service:
            name: flagger-grafana
            port:
              number: 8080
