name: innerloop
version: 1.0.0
summary: PiB Innerloop Environment
description: Deploys preconfigured innerloop vm
runner: ARM
templatePath: azuredeploy.json
parameters:
- id: "outerloop_vm"
  name: "outerloop_vm_vm"
  description: "Outerloop VM Name"
  default: "outerloop-vm"
  type: "string"
  required: true
- id: "pib_pat"
  name: "pib_pat"
  description: "Github PAT"
  default: ""
  type: "securestring"
  required: true
